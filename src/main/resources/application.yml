server:
  jetty:
    threads:
      #acceptors线程池用于接受HTTP请求
      acceptors: 4
      #selectors线程池用于处理HTTP数据包
      selectors: 8
      #worker线程池负责调用Web方法
      min: 8
      max: 200
  port: 7700
  servlet:
    context-path: /mis-api

spring:
  servlet:
    multipart:
      #允许上传文件
      enabled: true
      #每个文件最大不能超过20M
      max-file-size: 20MB
      #HTTP请求不能超过20M
      max-request-size: 20MB
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://39.105.19.136:7001/data?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
      username: root
      password: csy0420-
      initial-size: 8
      max-active: 16
      min-idle: 8
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: true
      test-on-return: false
      validation-query: SELECT 1
  data:
    mongodb:
      host: 39.105.19.136
      port: 7003
      database: data
      #admin是MongoDB用于验证用户身份的逻辑库
      authentication-database: admin
      username: admin
      password: abc123456
  redis:
    database: 0
    host: 39.105.19.136
    port: 7002
    password: csy0420-
    jedis:
      pool:
        #连接超时的最大时间
        max-active: 1000
        #等待空闲连接的最大等待时间（负数代表一直等待）
        max-wait: -1ms
        max-idle: 16
        min-idle: 8
  cache:
    redis:
      #缓存1个月时间
      time-to-live: 2592000000
    type: redis

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.example.db.pojo
  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true


minio:
  endpoint: http://39.105.19.136:7005
  access-key: root
  secret-key: csy0420-
  bucket: mis

sa-token:
  # token名称 (同时也是cookie名称)
  token-name: token
  # token有效期，单位s 默认30天, -1代表永不过期
  timeout: 2592000
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: 3600
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: false
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: false
  # token风格
  token-style: uuid
  # 是否输出操作日志
  is-log: false





